---
import { Waline } from "@components/Waline";

export interface Props {
  walineServer: string;
}

const { walineServer } = Astro.props;
---

<Waline
  serverURL={walineServer}
  reaction
  path={Astro.url.pathname}
  login="force"
  client:visible
/>
