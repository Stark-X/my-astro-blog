---
export interface Props {
  gTagId: string;
}
const { gTagId } = Astro.props;
---

<!-- Google tag (gtag.js) -->
<script
  is:inline
  async
  src={`https://www.googletagmanager.com/gtag/js?id=${gTagId}`}></script>
<script define:vars={{ id: gTagId }}>
  window.dataLayer = window.dataLayer || [];
  function gtag() {
    window.dataLayer.push(arguments);
  }
  gtag("js", new Date());
  gtag("config", id);
</script>
